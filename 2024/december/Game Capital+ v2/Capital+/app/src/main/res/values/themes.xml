<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Capital" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/yellow</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:navigationBarColor">@color/yellow</item>
    </style>

    <style name="Theme.Capital" parent="Base.Theme.Capital"/>
</resources>